
model foodType {
  id              Int           @id @default(autoincrement())
  name            String
  created_at      DateTime      @default(now()) @map("created_at")

  restaurants     Restaurant[]

  @@map("cuisine_types")
}